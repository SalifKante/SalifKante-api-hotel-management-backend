server:
  port: 8083
spring:
  datasource:
    username: root
    password: Salif123
    url: jdbc:mysql://localhost:3306/hotel_db
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      #format-sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      #max-size-threshold: 2KB
